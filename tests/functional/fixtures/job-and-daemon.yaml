version: '0.1'

settings:
  use-prefix: no

services:
  echo1:
    run: sleep 0.01 && echo "Job says I'm done"
  web1:
    # 0.3 simulates long-running
    run: sleep 0.3 && echo "Long running says I'm done"
